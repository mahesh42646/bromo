/**
 * Project entry — runs the API (TypeScript via `tsx` loader).
 *
 * From `bromo-api/`:
 *   npm start     → recommended (same as below)
 *   node --import tsx/esm server.ts
 *
 * Compiled production: npm run build && npm run start:prod
 */
import "./src/server.ts";
